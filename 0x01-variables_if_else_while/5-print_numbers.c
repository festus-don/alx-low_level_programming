#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 0 successs
 */
int main(void)
{
	int digit;

	for (digit = 0; digit <= 9; ++digit)
	{
		putchar(digit);
	}

	putchar('\n');

	return (0);
}
