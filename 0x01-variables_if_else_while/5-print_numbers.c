#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 0 successs
 */
int main(void)
{
	int digit;

	for(digit = 0; digit < 10; ++digit)
	{
		printf("%digit", digit);
	}

	printf("\n");

	return (0);
}
